# Database
DATABASE_URL="postgresql://user:password@localhost:5432/etsy_manager"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Etsy API (Opsiyonel - her mağaza için ayrı da tanımlanabilir)
ETSY_API_KEY=""
ETSY_API_SECRET=""

# Push Notifications (PWA/mobil bildirimler için gerekli)
# Anahtar üretmek için: npm run generate-vapid
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_SUBJECT="mailto:admin@example.com"
NEXT_PUBLIC_PWA_ENABLED="true"

# Email (SMTP) Ayarları
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
